#!/bin/sh

docker run --rm -v "$PWD:/workspace" openapitools/openapi-generator-cli:v7.4.0 generate \
  -i /workspace/openapi/openapi.yml \
  -g openapi-yaml \
  -o /workspace/openapi/merged

cd frontend
pnpm orval
