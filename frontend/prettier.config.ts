import type { Config } from "prettier";

const config: Config = {
  semi: true,
  trailingComma: "es5",
  singleQuote: false,
  printWidth: 130,
  tabWidth: 2,
  useTabs: false,
  endOfLine: "lf",
};

export default config;
